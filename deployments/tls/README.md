# TLS Assets

Place TURN TLS certificates here when using the container template:

- `turn.crt`
- `turn.key`

Production deployments should provision these files from the deployment secret
store, not commit private keys to the repository.
